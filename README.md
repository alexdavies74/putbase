# puter-fed

Monorepo containing:
- `packages/puter-federation-sdk`: TypeScript SDK plus Puter worker template.
- `packages/woof-app`: Vite TypeScript SPA using the SDK.

## Quick start

```bash
pnpm install
pnpm build
pnpm test
pnpm dev
```
